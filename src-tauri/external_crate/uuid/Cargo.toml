[package]
name = "uuid"
version = "0.1.0"
edition = "2024"

[dependencies]
uuid = { version = "=1.14.0", features = [
    "v7",
    "fast-rng",
    "macro-diagnostics",
    "serde",
] }
sqlx = { workspace = true, features = ["uuid"] }
serde.workspace = true
