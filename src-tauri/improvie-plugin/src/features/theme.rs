#[derive(Debug, Clone, serde::Deserialize, serde::Serialize)]
pub struct ThemeFeature<'a> {
    pub name: &'a str,

    pub background: &'a str,
    pub foreground: &'a str,
    pub muted: &'a str,
    pub muted_foreground: &'a str,
    pub popover: &'a str,
    pub popover_foreground: &'a str,
    pub card: &'a str,
    pub card_primary: &'a str,
    pub card_foreground: &'a str,
    pub border: &'a str,
    pub input: &'a str,
    pub primary: &'a str,
    pub primary_foreground: &'a str,
    pub secondary: &'a str,
    pub secondary_foreground: &'a str,
    pub accent: &'a str,
    pub accent_foreground: &'a str,
    pub destructive: &'a str,
    pub destructive_foreground: &'a str,
    pub ring: &'a str,
    pub radius: &'a str,
    pub sidebar_background: &'a str,
    pub sidebar_foreground: &'a str,
    pub sidebar_primary: &'a str,
    pub sidebar_primary_foreground: &'a str,
    pub sidebar_accent: &'a str,
    pub sidebar_accent_foreground: &'a str,
    pub sidebar_border: &'a str,
    pub sidebar_ring: &'a str,
}
